{
  "name": "ruler-protocal",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ developerfred/ruler-protocal",
    "create-local": "graph create --node http://localhost:8020/ developerfred/ruler-protocal",
    "remove-local": "graph remove --node http://localhost:8020/ developerfred/ruler-protocal",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 developerfred/ruler-protocal"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
